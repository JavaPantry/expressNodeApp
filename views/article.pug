extends layout 

block content
    //- h1 #{article.title} 
    h1= article.title
    h5 Written By #{article.author} 
    hr
    p= article.body
    hr
    a.btn.btn-primary(href='/article/edit/'+article._id) Edit
    | 
    a.btn.btn-outline-success(href='/') Back Home